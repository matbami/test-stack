/**
 * Enum representing the target type for actions such as voting or rating.
 */
export enum TargetType {
    QUESTION = 'question',
    ANSWER = 'answer',
  }
  
  /**
   * Enum representing the type of vote.
   */
  export enum Vote {
    UPVOTE = 'upvote',
    DOWNVOTE = 'downvote',
  }
  